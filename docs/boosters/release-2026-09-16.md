# Booster release preparation — 16 September 2026

**Backend published and live verified on 16 September 2026.** The shared catalog now serves 15 products, including all ten boosters, to both app and website. Website deployment and the installed-app update remain separate and pending.

## Website

An isolated copy of the currently checked-out release contains the booster artwork, localized product names, concise descriptions and expandable product details. It excludes the other session's uncommitted authentication changes. TypeScript, 14 catalog/copy tests and the Next.js production build passed. All ten app and website booster image files match byte for byte.

Publishing to Vercel project `seekprotocal-web` was rejected by automatic approval review because it required explicit approval for that external destination. A request for `www.seekprotocol.ai` / `seekprotocol.ai` is pending. No deployment was created and no domains were changed.

## Installed app compatibility

Live EAS inspection found finished SDK56 builds through 2.2.1, while the current development source uses SDK57. A separate SDK56 release was prepared from the source of the currently published Home fix. It preserves the malformed-spawn guard and native dependencies, includes booster artwork/copy, map/AR presentation and gameplay UI changes. TypeScript, 79 focused tests and both Hermes exports passed.

**Do not publish this export yet:** the installed SDK56 app uses the older native-store checkout, whereas current development uses a different commerce integration. The staged dynamic catalog needs its billing behavior and displayed prices reconciled with that installed checkout before publication. A successful export does not prove purchase support. No EAS update, native build, or store submission was published. No physical-device verification was completed.

## Backend

Published only the booster changes to Supabase project `ukoqxyoogjouhdqhtiei`. Five migrations are applied: `20260916100000`, `20260916101000`, `20260916102000`, `20260916104000` and `20260916105000`. No migration history was repaired and no unrelated pending migration was included.

The first migration stages the four new products and effects inactive. The activation/audit schema, prices and single-use replay guard were applied first. The five handlers were then published and checked before the final migration enabled the products. The published versions are `get-spawns` 188, `reveal-spawns` 17, `collect-coin` 188, `activate-powerup` 94 and `activate-lure` 104. Their gateway JWT settings are unchanged.

Live verification confirms identical app/web catalogs with 15 products and ten boosters; Cash EUR0.79, Fitness EUR1.49, Chill Guy EUR1.99 and This Is Fine EUR2.99; one matching stock grant per new product; four active 15-minute effect definitions; and catalog activation audit records. Requests without a user session cannot activate or collect a booster, and direct client calls to the privileged activation RPC are rejected. No customer purchase or inventory was created for these checks.

The disposable PostgreSQL suite passed again with the staged-release check: repeated payment callbacks deliver once, eight concurrent activations reserve one unit, prices and audit records agree, historical orders/disabled products are preserved, private usage rows are protected, Fitness range/expiry is enforced, and one shield cannot rescue two simultaneous catches. The temporary database server was stopped.

The actual website parser accepts the live 15-product catalog. All four new illustration files are served successfully by localhost and match their app counterparts byte for byte. The browser is now on `/nl/shop`, but its session is logged out, so an authenticated visual check and real payment were not performed. Local users can log in and reload the shop to receive the newly published catalog.

Evidence: `backend-live-pre-release.json`, `backend-live-final.json`, `functions-published.json`, `migrations-published.txt` and `database-tests-final.log` in the temporary release directory.

## 3D quality

The prepared presentation still uses the original models with selected animations, corrected orientation, material refinements and shared effects. It does not contain ten meshes rebuilt to match the new illustrations.

Two experimental replacement meshes for Diamond Hands and This Is Fine were authored and rendered locally. Their quality is not approved for production. They remain under the temporary release directory and are not wired into app assets, model URLs or storage. Do not describe them as finished assets.

## Evidence

Machine-readable status: `release-2026-09-16.json`. Build logs, exported bundles, downloaded handler sources and database-test output are in `/private/tmp/seek-booster-rollout-20260916`.
