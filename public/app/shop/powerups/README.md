These transparent 512px PNGs are rendered directly from the app map GLBs. Source hashes, poses, hidden meshes and viewing angles are recorded in source-manifest.json.

Mapping follows seekar-app/lib/powerups/models.ts: rare_boost → shiba, spawn_lure → whale, xp_boost → to_the_moon. Other keys use their matching model. To The Moon uses the Bitcoin_Rig|Scene.002 animation pose. Model fitting follows components/map/webview/models.ts.

The same PNG bytes are used in seekar-app/assets/powerups/thumbnails. Cardpack artwork is unchanged. ../pass-hero.jpg is the original seekar-app/assets/shop/pass-hero.jpg used by PassCard.tsx.
